using Xunit;

namespace ElBruno.Speech.IntegrationTests;

// Integration tests — implementation coming in Phases 3–5.
// Tests in this project are tagged [Trait("Category","Integration")] and excluded from default CI.
public class IntegrationPlaceholderTests
{
    [Fact]
    [Trait("Category", "Integration")]
    public void Placeholder_passes() => Assert.True(true);
}
